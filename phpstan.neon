includes:
    - ./vendor/larastan/larastan/extension.neon

parameters:
    paths:
        - app
        - config
        - database
        - routes
    level: 8
    ignoreErrors:
        # Add any error patterns to ignore here if needed
    excludePaths:
        # Add any paths to exclude here if needed
